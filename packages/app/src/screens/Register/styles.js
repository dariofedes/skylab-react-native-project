import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 30,
    },
    icon: {
        alignSelf: 'center',
        height: 100,
        width: 100
    },
    formContainer: {
        marginTop: 30,
        marginBottom: 30
    },
})

export default styles